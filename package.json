{
  "name": "react-webpack-boilerplate",
  "description": "Simple boilerplate for a modern Webpack & React developer experience.",
  "version": "1.0.0",
  "author": "Jack R. Zhang < jackrzhang@college.harvard.edu >",
  "keywords": [],
  "private": true,
  "scripts": {
    "start": "node webpack/index | NODE_ENV=development node src/server",
    "clean": "rm -rf build",
    "build": "npm run clean && NODE_ENV=production webpack --config webpack/config.prod.js",
    "production": "npm run buuld && NODE_ENV=production node src/server",
    "lint": "./node_modules/.bin/eslint webpack src || true",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "devDependencies": {
    "babel-core": "^6.10.4",
    "babel-loader": "^6.2.4",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.11.1",
    "babel-preset-stage-0": "^6.5.0"
  }
}
